brew -v
brew install git
git —version

rvm
curl -L get.rvm.io | bash -s stable
	CLOSE AND OPEN NEW WINDOW
	type rvm | head -1
	rvm -v

rvm autolibs homebrew
rvm install 2.3
rvm use 2.3
rvm --default use 2.3
	ruby -v
gem install rails
	rails -v
which ruby
